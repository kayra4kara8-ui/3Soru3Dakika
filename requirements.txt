# Zorunlu
streamlit>=1.32.0
requests>=2.31.0

# Video oluşturma için (opsiyonel ama önerilir)
Pillow>=10.0.0

# FFmpeg sistem kurulumu gerekir (pip değil):
# Linux: sudo apt install ffmpeg
# macOS: brew install ffmpeg
# Windows: https://ffmpeg.org/download.html
